machine:
  pre:
    - curl https://raw.githubusercontent.com/creationix/nvm/v0.23.3/install.sh | bash
  node:
    version: 0.12

dependencies:
  override:
    - npm install -g bower
    - npm install
    - bower install
