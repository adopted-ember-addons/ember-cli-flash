<h1 id="title">Ember-CLI-Flash Demo</h1>

<p>Simple, highly configurable flash messages for ember-cli.</p>

{{#each flashMessages.arrangedQueue as |flash|}}
  {{#flash-message flash=flash as |comp flash|}}
    <h6>{{comp.flashType}}</h6>
    <p>{{flash.message}}</p>
    {{#if comp.showProgressBar}}
      <div class="alert-progress">
        <div class="alert-progressBar" style="{{comp.progressDuration}}"></div>
      </div>
    {{/if}}
  {{/flash-message}}
{{/each}}

<code><pre>
  \{{#each flashMessages.arrangedQueue as |flash|}}
    \{{#flash-message flash=flash as |comp flash|}}
        &lt;h6&gt;\{{compon.flashType}}&lt;/h6&gt;
    &lt;p&gt;\{{flash.message}}&lt;/p&gt;
      \{{#if comp.showProgressBar}}
          &lt;div class="alert-progress"&gt;
              &lt;div class="alert-progressBar" style="{{comp.progressDuration}}"&gt;&lt;/div&gt;
          &lt;/div&gt;
      \{{/if}}
    \{{/flash-message}}
  \{{/each}}

</pre></code>
